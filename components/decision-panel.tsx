import { CheckCircle2, FileSignature, Fingerprint, Scale, ShieldAlert } from "lucide-react";
import { AuthorityStrip } from "@/components/authority-strip";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { humanDecisionCanSign } from "@/lib/protocol";
import type { ContractState } from "@/lib/case-model";

type Checklist = keyof ContractState["humanDecision"]["checklist"];
type Props = {
  state: ContractState; busy: boolean; notice: string;
  toggleSettlementConsent: (party: "supplier" | "customer") => void; requestSettlement: () => void;
  respondSettlement: (party: "supplier" | "customer", response: "accept" | "decline") => void;
  updatePreliminary: (value: string) => void; runDetermination: (mode: "illustrative" | "live") => void;
  updateDecision: (field: "status" | "rationale", value: string) => void;
  toggleChecklist: (field: Checklist) => void; signDecision: () => void;
};

export function DisputePreview({ state, busy, notice, toggleSettlementConsent, requestSettlement, respondSettlement, updatePreliminary, runDetermination, updateDecision, toggleChecklist, signDecision }: Props) {
  const simulationAvailable = ["appointment_simulated", "dispute_simulated", "closed"].includes(state.lifecycleStatus);
  const disputeOpen = state.lifecycleStatus === "appointment_simulated";
  const proposal = state.settlement.proposal;
  const determination = state.proposedDetermination;
  const noticeFailed = /failed|disabled|unavailable|mismatch|timed out/i.test(notice);

  return <>
    <div className="page-intro"><span>Stage 5 · later synthetic dispute</span><h1>Apply the acknowledged protocol to a later dispute—without surrendering human control.</h1><p>The synthetic record is bound to the exact manifest hash. Settlement stays sealed from merits. Protocol output remains provisional; the fictional human decision remains non-operative and without legal effect.</p></div>
    {!simulationAvailable && <div className="warning"><ShieldAlert size={18} /><div><strong>No simulated appointment record</strong><p>Complete the Exact protocol manifest and integrity-verified simulated ceremony before opening this synthetic dispute.</p></div></div>}

    <div className={!simulationAvailable ? "stage-disabled" : ""}>
      <Card className="manifest-binding"><div><Fingerprint size={20} /><span>01 · acknowledged protocol identified</span><h2>Later dispute bound to the simulated appointment record</h2></div><dl><div><dt>Manifest hash</dt><dd>{state.dispute.appointmentHash ?? "Not yet bound"}</dd></div><div><dt>Constitution</dt><dd>v{state.constitution.version}</dd></div><div><dt>Lifecycle</dt><dd>{state.lifecycleStatus}</dd></div><div><dt>Effect</dt><dd>Simulation only · no legal effect</dd></div></dl></Card>

      <Card className="dispute-record"><div className="finding-top"><div><Badge tone="red">02 · Shared merits record</Badge><h2>Later SaaS outage</h2></div><span className="version">{state.dispute.id}</span></div><AuthorityStrip executionStatus="not_executed" actor="Supplier, customer, and showcase curator" version="Synthetic dispute v1" consequence="Defines the shared merits record for the simulation" provenance="Curated synthetic claim, defence, evidence, objections, and gaps" /><div className="position-grid"><div><small>Customer claim · party assertion</small><p>{state.dispute.claim}</p></div><div><small>Supplier defence · party assertion</small><p>{state.dispute.defence}</p></div></div><div className="stress-input-grid"><div><small>Shared evidence</small><ul>{state.dispute.sharedEvidence.map((item) => <li key={item}>{item}</li>)}</ul></div><div><small>Objections</small><ul>{state.dispute.objections.map((item) => <li key={item}>{item}</li>)}</ul></div><div><small>Missing evidence</small><ul>{state.dispute.missingEvidence.map((item) => <li key={item}>{item}</li>)}</ul></div></div></Card>

      <Card className="settlement-card"><Badge tone="blue">03 · Optional sealed simulation track</Badge><h2>Settlement Facilitation</h2><p>This track is separate from Party Alignment and merits reasoning. Proposal content and responses never enter `buildAdjudicationInput`; only permitted status information may reach the shared record.</p><div className="confirm-row"><span>Bilateral simulation activation</span><div>{(["supplier", "customer"] as const).map((party) => <Button key={party} disabled={!disputeOpen || Boolean(determination)} variant={state.settlement.consents[party] ? "secondary" : "ghost"} onClick={() => toggleSettlementConsent(party)}>{state.settlement.consents[party] && <CheckCircle2 size={14} />} {party}</Button>)}</div></div><Button disabled={!disputeOpen || Boolean(determination) || !state.settlement.consents.supplier || !state.settlement.consents.customer || Boolean(proposal)} onClick={requestSettlement}>{busy ? "Preparing sealed proposal…" : "Generate non-binding proposal"}</Button>
        {proposal && <div className="sealed-proposal"><Badge tone="red">Sealed from merits</Badge><h3>{proposal.label}</h3><ul>{proposal.terms.map((term) => <li key={term}>{term}</li>)}</ul><p>{proposal.basis}</p><div className="response-grid">{(["supplier", "customer"] as const).map((party) => <div key={party}><strong>{party}</strong><Button variant="secondary" disabled={!disputeOpen} onClick={() => respondSettlement(party, "accept")}>Accept</Button><Button variant="ghost" disabled={!disputeOpen} onClick={() => respondSettlement(party, "decline")}>Decline</Button><small>{state.settlement.responses[party]}</small></div>)}</div></div>}
        <div className="settlement-status"><span>Status</span><strong>{state.settlement.status}</strong><p>{state.settlement.status === "not_settled" ? "Facilitation ended without settlement. Proposal content remains outside merits." : state.settlement.status === "settled" ? "Synthetic settlement recorded; adjudication is closed." : "The merits record contains no settlement proposal or response content."}</p></div>
      </Card>

      <Card className="human-preassessment"><Badge>04 · Fictional human pre-assessment</Badge><h2>Record an independent initial view before revealing protocol output.</h2><p>This sequencing demonstrates human independence; it does not prove a real decision-maker performed the review.</p><textarea disabled={!disputeOpen || Boolean(determination)} value={state.humanDecision.preliminaryAssessment} onChange={(event) => updatePreliminary(event.target.value)} placeholder="Fictional arbitrator’s preliminary issue assessment…" /><div className="action-row"><Button variant="secondary" disabled={!disputeOpen || state.settlement.status === "settled" || busy || !state.humanDecision.preliminaryAssessment.trim()} onClick={() => runDetermination("illustrative")}>View illustrative example</Button><Button disabled={!disputeOpen || state.settlement.status === "settled" || busy || !state.humanDecision.preliminaryAssessment.trim()} onClick={() => runDetermination("live")}>{busy ? "Running execution…" : "Run live execution"}</Button></div>{notice && <div className={noticeFailed ? "warning" : "notice"} role="status">{noticeFailed && <ShieldAlert size={16} />}<span>{notice}</span></div>}</Card>

      {determination && <Card className="determination-card"><Badge tone="blue">05 · Provisional simulation-only determination</Badge><h2>{determination.proposedDisposition}</h2><AuthorityStrip executionStatus={determination.metadata.executionStatus} actor={determination.metadata.executionMode === "live" ? "Declared live model" : "Showcase curator"} version={determination.metadata.artifactId} consequence="Supports fictional human review only; cannot operate as an award" provenance={determination.metadata.provenance} /><p>{determination.reasoningSummary}</p><div className="stress-input-grid"><div><small>Findings</small><ul>{determination.findings.map((item) => <li key={item}>{item}</li>)}</ul></div><div><small>Counterarguments</small><ul>{determination.counterarguments.map((item) => <li key={item}>{item}</li>)}</ul></div><div><small>Uncertainty and escalation</small><ul>{[...determination.uncertainty, ...determination.escalationFlags].map((item) => <li key={item}>{item}</li>)}</ul></div></div><span className="version">Bound to {determination.appointmentHash}</span></Card>}

      {determination && <Card className="human-decision"><Badge tone="red">06 · Fictional human control · no legal effect</Badge><h2>Adopt, modify, or reject the provisional artifact independently.</h2><div className="option-row">{(["adopted", "modified", "rejected"] as const).map((status) => <button key={status} className={state.humanDecision.status === status ? "option selected" : "option"} onClick={() => updateDecision("status", status)}><span>{status}</span><small>The fictional record preserves a distinct human rationale.</small></button>)}</div><label className="clause-editor"><span>Independent simulated rationale</span><textarea value={state.humanDecision.rationale} onChange={(event) => updateDecision("rationale", event.target.value)} /></label><div className="review-checklist">{(Object.keys(state.humanDecision.checklist) as Checklist[]).map((field) => <label key={field} className="checkline"><input type="checkbox" checked={state.humanDecision.checklist[field]} onChange={() => toggleChecklist(field)} /> {field.replace(/([A-Z])/g, " $1")}</label>)}</div><Button disabled={!humanDecisionCanSign(state)} onClick={signDecision}><FileSignature size={15} /> Record simulated human decision</Button></Card>}

      {state.humanDecision.simulatedSignature && <div className="result success final-award"><Badge tone="green">Simulated human decision · non-operative</Badge><strong>{state.humanDecision.status}</strong><span>{state.humanDecision.simulatedSignature}</span><p><Scale size={14} /> Fictional review record only · no production signature, award, or legal effect.</p></div>}
    </div>
  </>;
}
